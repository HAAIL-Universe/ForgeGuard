# FORMAT SPECIFICATION ONLY
# All endpoints, schemas, and values below are placeholders.
# Do NOT use path names, entity names, or schema names from this skeleton.
# All paths must be derived from the user's actual API requirements and schema entities.
# Auth method must match the canonical anchor (none / token / github-token).
# Schema names must match table names from the schema contract.

info:
  title: "[PROJECT NAME] API"
  version: "1.0.0"
  description: "[Brief description of what this API does]"

paths:

  # Replace [resource] with the actual domain entity name (from schema contract)
  /api/v1/[resource_plural]:
    get:
      summary: "List [resource]"
      auth: token
      query_params:
        - name: limit
          type: integer
          default: 50
      response:
        200:
          items:
            - id: uuid
              created_at: timestamptz

    post:
      summary: "Create [resource]"
      auth: token
      request_body:
        required_field: "[type]"
        optional_field: "[type]"
      response:
        201:
          id: uuid
          created_at: timestamptz

  /api/v1/[resource_plural]/{id}:
    get:
      summary: "Get [resource] by ID"
      auth: token
      response:
        200:
          id: uuid
          created_at: timestamptz
        404:
          detail: "Not found"

    put:
      summary: "Update [resource]"
      auth: token
      request_body:
        field_name: "[type]"
      response:
        200:
          id: uuid

    delete:
      summary: "Delete [resource]"
      auth: token
      response:
        204: {}

schemas:
  EntityName:
    id: uuid
    field_name: "[type]"
    created_at: timestamptz
    updated_at: timestamptz
